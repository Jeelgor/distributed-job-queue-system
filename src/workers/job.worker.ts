import { logger } from '../utils/logger';

// Placeholder for background job worker
// Wire up a queue (e.g. BullMQ, pg-boss) here later

export async function startJobWorker() {
  logger.info('Job worker started (placeholder)');
}
